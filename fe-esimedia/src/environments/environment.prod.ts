// Environment for production
export const environment = {
  production: true,
  // Usa una URL vacía para que las peticiones vayan al mismo dominio
  // Esto funciona porque el backend sirve el frontend desde la misma URL
  apiUrl: ''
};
